coverage:
  status:
    patch:
      default:
        target: 100%
        threshold: 0%
    project:
      default:
        informational: true

ignore:
  - "tests/**/*"
  - "scripts/**/*"

comment:
  layout: "reach, diff, flags, files"
  require_changes: true
